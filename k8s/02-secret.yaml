---
# POC-only development credentials — NOT for production use.
# These are the same throwaway defaults used in docker-compose (H2 sa/empty,
# MinIO minioadmin/minioadmin). Never reuse these values outside this local POC.
apiVersion: v1
kind: Secret
metadata:
  name: report-composer-secret
  namespace: report-composer
  labels:
    app.kubernetes.io/name: report-composer
type: Opaque
stringData:
  DB_USER: "sa"
  DB_PASSWORD: ""
  MINIO_ACCESS_KEY: "minioadmin"
  MINIO_SECRET_KEY: "minioadmin"
